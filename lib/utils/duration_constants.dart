abstract class DurationConstants {
  static const page = Duration(seconds: 7);
  static const transition = Duration(seconds: 1);
  static const hide = Duration(milliseconds: 300);
  static const show = Duration(milliseconds: 300);
}
